// MD React Base Styles
import colors from "assets/theme/base/colors";

const { text, info } = colors;

const iconButton = {
  styleOverrides: {
    root: {
      color: text.main, // Color on hover
    },
  },
};

export default iconButton;
